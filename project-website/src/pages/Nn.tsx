import ModelScaffold from "../components/ModelScaffold";

const Nn = () => (
  <ModelScaffold
    title="Neural Networks"
    module="Module 5"
    plan="A neural network will model the more complex, non-linear relationships between conditions and air quality, and its accuracy will be compared with the simpler methods."
  />
);

export default Nn;
