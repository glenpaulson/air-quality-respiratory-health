import ModelScaffold from "../components/ModelScaffold";

const Regression = () => (
  <ModelScaffold
    title="Regression"
    module="Module 5"
    plan="Linear regression will estimate a continuous air-quality value from weather and pollutant conditions, and quantify how each factor contributes."
  />
);

export default Regression;
