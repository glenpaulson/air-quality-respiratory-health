import ModelScaffold from "../components/ModelScaffold";

const Pca = () => (
  <ModelScaffold
    title="PCA"
    module="Module 2"
    plan="Principal Component Analysis will reduce the many correlated pollutant and weather measurements into a few underlying dimensions, making the structure of the data easier to see and to model."
  />
);

export default Pca;
