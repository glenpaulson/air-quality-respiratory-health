import ModelScaffold from "../components/ModelScaffold";

const DecTrees = () => (
  <ModelScaffold
    title="Decision Trees"
    module="Module 3"
    plan="Decision trees will produce readable rules that predict a day's air-quality category and highlight which conditions matter most."
  />
);

export default DecTrees;
