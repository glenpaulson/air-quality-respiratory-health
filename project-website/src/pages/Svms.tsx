import ModelScaffold from "../components/ModelScaffold";

const Svms = () => (
  <ModelScaffold
    title="Support Vector Machines"
    module="Module 4"
    plan="Support Vector Machines with several kernels will be compared on the task of separating healthy from unhealthy-air days."
  />
);

export default Svms;
