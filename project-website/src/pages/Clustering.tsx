import ModelScaffold from "../components/ModelScaffold";

const Clustering = () => (
  <ModelScaffold
    title="Clustering"
    module="Module 2"
    plan="Clustering will group cities and city-days that share similar pollution and weather profiles, revealing natural air-quality 'types' without using any labels."
  />
);

export default Clustering;
