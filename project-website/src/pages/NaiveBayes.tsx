import ModelScaffold from "../components/ModelScaffold";

const NaiveBayes = () => (
  <ModelScaffold
    title="Naive Bayes"
    module="Module 3"
    plan="A Naive Bayes classifier will estimate the probability that a given day falls into each air-quality category based on its conditions."
  />
);

export default NaiveBayes;
